name: Run commands on different operating systems
on:
  push:
    branches: [ master ]

jobs:
  test_job:
    runs-on: ubuntu-latest

    steps: 
      - name: "Primera prueba"
        run: echo "Primera prueba"    
