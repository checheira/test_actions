name: Run commands on different operating systems
on: # cuando ejecutar
  push:
    branches: [ ibs ]

jobs:
  test_job:
    runs-on: ubuntu-latest # utilizar una versión concreta

    steps: 
      - name: "Primera prueba"
        run: echo "Primera prueba"    
      - uses: actions/checkout@v4 # Obtener el ultimo commit de un repositorio
        with:
          fetch-depth: 0
          